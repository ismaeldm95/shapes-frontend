import { atom } from 'jotai'

export const isRoundEndedAtom = atom(false)
export const isPreRoundCountdownAtom = atom(true)
export const isRoundActiveAtom = atom(false)
export const showFeedbackAtom = atom(false)
